# keycloak-tls-secret.yaml

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: keycloak-tls-secret
  namespace: keycloak
spec:
  secretName: keycloak-tls-secret
  commonName: keyc.kube5s.ru
  dnsNames:
    - keyc.kube5s.ru
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: letsencrypt-prod
  usages:
    - server auth
    - client auth